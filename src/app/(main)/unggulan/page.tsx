import { Metadata } from "next";

export const metadata : Metadata = {
  title : "Unggulan"
}

const Page = () => {
  return (
    <>
      <section>This is Unggulan Page</section>
    </>
  );
};

export default Page;
